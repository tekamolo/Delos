{% extends 'base.html.twig' %}

{% block title %}Main Template{% endblock %}

{% block css %}
{% endblock %}

{% block js %}
{% endblock %}

{% block content %}
    {% if users is not empty %}
        {% for u in users %}
            <p>{{ u.username }} - {{ u.email }}</p>
        {% endfor %}
    {% else %}
        {{ dump(users) }}
    {% endif %}
{% endblock %}